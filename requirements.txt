# ─────────────────────────────────────────────────────────────────────────────
# Olist Customer Behavior Pipeline
# Airflow itself is provided by the Docker base image.
# ─────────────────────────────────────────────────────────────────────────────

# PostgreSQL
psycopg2-binary>=2.9

# Airflow providers
apache-airflow-providers-postgres
apache-airflow-providers-standard
apache-airflow-providers-fab

# Data / ML
pandas>=2.2,<3
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3

# Testing
pytest>=8
